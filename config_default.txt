# Default config for A-Maze-ing
WIDTH=20
HEIGHT=15
ENTRY=0,0
EXIT=19,14
OUTPUT_FILE=maze.txt
PERFECT=False

# Bonus-ready extras (optional; safe defaults exist)
SEED=42
ALGORITHM=dfs         # dfs|prim|kruskal|wilson
DISPLAY=ascii         # ascii (recommended)
ANIMATE=False         # legacy toggle (applies to both solver & generator)
ANIMATE_SOLVER=False  # animate solver pathfinding steps
ANIMATE_GENERATION=False  # animate maze generation steps
STEP_DELAY_MS=25      # for animation (ms per frame)
DENSITY=0.06          # float between 0.0 and 1.0
